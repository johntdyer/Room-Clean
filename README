VoiceXML target is obviously vxmlApplication.xml       

Setup: 
	-Deploy to /opt/voxeo/webapps/www
	-Application type: VXML
	-Target URL http://127.0.0.1:9990/roomClean/vxmlApplication.xml

Use:

Key in room numbers (DTMF Only), delimited by #.  Once done the API will simulate a successful or failed DB update.  If failed it will prompt to re-input room numbers.  We also will log room numbers (CSV) for illustrative purposes.


